[package]
name = "drift"
version = "0.1.0"
edition = "2021"

[dependencies]
clap = { version = "4", features = ["derive"] }
clap_complete = "4"
drift-core = { path = "../drift-core" }
serde_json = "1"
